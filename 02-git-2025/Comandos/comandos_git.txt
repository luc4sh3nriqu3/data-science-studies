git init. 		-> Cria um repo git na pasta atual
git status 		-> Mostra o status do trabalho (de cada arquivo)
git add nome_arquivo 	-> Adiciona arquivo para ser commitado
git commit -m "msg" 	-> Faz commit do que estava para ser commitado
git log -> mostra todo o histórico de commits

git diff -> Mostra as diferenças no arquivo desde o último commit
git diff . -> Faz isso pra todos os arquivos
git reset nome_arquvivo> -> Retira o arquivo do status de stage (quando damos um git add), ou seja, retira ele para ser commitado
git checkout -b nome_branch -> Cria uma nova branch
git branch -> Mostra todas as branchs do seu projeto, além de mostrar em qual branch você está
git branch -D nome_branch -> Deleta a branch especificada

========================================= GIT HUB =========================================
git remote add origin https://github.com... -> Vincula um repositório remoto (endereço especificado) ao nosso repositório local
git push origin main -> Enviando para a nossa origem/repo_remoto as alterações que ocorreram na branch main (Alterações que foram realizadas desde a criação do repositório local). Basicamente estamos atualizando o repositório remoto com tudo o que o nosso repositório local possui
===========================================================================================
#Fodase
